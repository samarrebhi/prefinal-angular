import { ChambredirectiveDirective } from './chambredirective.directive';

describe('ChambredirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChambredirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
