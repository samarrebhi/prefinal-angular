import { BlocdirectiveDirective } from './blocdirective.directive';

describe('BlocdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
